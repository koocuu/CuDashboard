## 2026-07-13 · 选题页与中文标题

- **改动**：首页「今日选题」改为精简预览（每账号最多 2 条）+ 链到 `/topics` 全文页；顶栏增加「选题」；topic-radar 打分新增 `title_zh`。
- **原因**：首页小框塞全文难读；源标题多为英文，需要中文工作标题。
- **验证**：`npx tsc --noEmit`；topic-radar Actions `29223454508` ✅ → batch #2；`GET /api/topic-batches` 10/10 候选均有中文 `title_zh`。刷新首页与 `/topics` 即可。

## 2026-07-13 · 选题改首页区块（非画像提案）

- **改动**：新增 `topic_batches` 表与 `POST/GET /api/topic-batches`；首页「今日选题」展示最新批次；撤回误加的画像层 `topics`。
- **原因**：画像提案只应用于画像更新；选题雷达应是首页可浏览的候选列表。
- **影响文件**：`lib/db/schema.ts`、`drizzle/0010_topic_batches.sql`、`lib/queries/topics.ts`、`app/api/topic-batches/route.ts`、`middleware.ts`、`components/dashboard/topic-radar-panel.tsx`、`app/(app)/dashboard/page.tsx`、画像层相关回退
- **验证**：`npx tsc --noEmit`；需执行 `npm run db:migrate` 后部署
- **你后续**：部署 + 迁移；拒绝旧的 topics 画像提案；再跑一次 topic-radar Actions

## 2026-07-13 · topic-radar 对接

- **改动**：新增画像层 `topics`（内容选题候选）；MCP / 粘贴协议 / propose API 校验文案同步支持；完整版分发默认包含 `topics`，通用版不含。
- **原因**：独立服务 topic-radar 每日通过已有 `POST /api/profile/proposals` 写入 pending proposal，不新建并行写入通道。
- **影响文件**：`lib/db/schema.ts`、`lib/profile-meta.ts`、`app/api/mcp/route.ts`、`app/api/profile/proposals/route.ts`、`lib/profile-update-parser.ts`、`lib/profile-update-protocol.ts`
- **验证**：`npx tsc --noEmit` 通过（无 DB migration：layer 为 text）
- **你后续**：部署本改动；在设置页创建 write token（名如 `topic-radar`）交给 topic-radar 的 `CONSOLE_API_TOKEN`；每天在「画像提案」批准 `topics` 即可

---

# 开发进度 · Console v2

> 当前实施口径依据 `PRD-个人知识库系统.md` v2.0,最后更新:2026-07-11。

## 总览

| Phase | 内容 | 状态 |
|---|---|---|
| Phase 1 | 登录、首页、工作台账、PWA | ✅ 完成 |
| Phase 2 | 五层画像、分享页、提案、持仓 | ✅ 完成 |
| Phase 3 | 远程 MCP、OAuth、月度投资审计 | ✅ 完成 |

## 当前产品口径

- 首页桌面双栏、移动单列;底部 Tab 和独立设置页已删除。
- 工作事项只有 `someday / scheduled / in_progress / done` 四态;点击圆点前进,支持跨栏拖拽、置顶和轻分类。
- 快速录入默认进入“想做未做”;置顶和状态是独立字段。
- 画像分发以 `/c/<slug>` 分享页和复制文本为主;MCP `get_profile` 返回无包装纯净内容。
- 画像写入统一走 proposal → diff → 批准;粘贴协议保持严格解析。
- 持仓以 `amount_cny` 为主数据,名称和金额可编辑;比例由全部资产金额自动计算。
- 现金以 `CASH` 显式计入总资产;环图切片合计 100%,中心显示非现金已投资比例。
- 2026-07 当前快照:总资产 `¥904,646`,现金 `5.53%`,已投 `94.47%`。
- 月度投资更新只通过 MCP `propose_monthly_investment_update` 提交;固定四段审计与完整金额持仓一次批准、同节点落库。
- MCP 另外提供 `get_profile`、`search_entries`、`propose_profile_update`;支持 OAuth 和静态 Bearer token。
- Neon 迁移已到 `0009_holding_amount_monthly_review`;同步脚本会更新 7 月金额并刷新当月快照。

## 验证状态

- ✅ `npx tsc --noEmit`
- ✅ `npm run build`
- ✅ 金额样例计算:九项 `¥904,646`,切片合计 `100.00%`,现金 `5.53%`,已投 `94.47%`
- ✅ `git pull --rebase origin main` 已确认远端最新

## 线上硬验收

- [x] Neon 已执行金额/月度提案迁移并同步 2026-07 数据
- [x] 7 月月度审计保存金额快照
- [x] Claude.ai OAuth 可连接 `/api/mcp`
- [ ] 部署本次“已投比例”修正后,首页与投资页中心显示 `94.47%`
- [ ] Claude.ai 刷新连接器后能看到并调用 `propose_monthly_investment_update`
- [ ] 用一条测试月度提案走完:提交 → 投资页 diff → 批准 → 持仓与复盘同时更新
- [x] 私有仓库 `koocuu/CuDashboard-backup` 已创建,2026-07-11 手动初始备份成功(41 个 Markdown)
- [ ] 在 Vercel Production 配置仅限该仓库的 fine-grained `GITHUB_BACKUP_TOKEN`,再验证每日 Cron

## 月更操作

1. 把当月真实金额和审计材料交给支持 MCP 的 AI。
2. AI 调用 `propose_monthly_investment_update`,必须包含全部资产及 `CASH`。
3. 在投资页核对总资产、每项金额、比例变化和四段总结。
4. 批准后系统同步持仓并固化当月快照;不再手工粘贴复盘。
