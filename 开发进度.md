# 开发进度 · CuDashboard

> 追加式记录；多 agent 并行时不要覆盖他人条目。开工前先读本文件与 `PRD-个人知识库系统.md`。

## 2026-08-17 · 作品页去掉叠在一起的双横线

- **改动**：目录已有 `border-b`，详情又加了 `border-t`，中间出现两条线。去掉详情顶边，只留目录底下一根。
- **原因**：用户看到作品页两条横线。
- **影响文件**：`components/projects/project-wall.tsx`、`开发进度.md`。
- **验证**：未跑页面，改动是去掉重复 border。
- **后续**：推 main 后刷新「项目」页。

## 2026-08-17 · 投资进顶栏，今日去掉仓位摘要

- **改动**：顶栏改为「今日 / 项目 / 投资 / 画像」。投资待审角标挂在投资 Tab 上。今日左侧仓位环图和页脚入口删除。选题改为顶栏右侧淡链，不进主导航。近况默认只露「在做」，其余进展开全文，减轻左右栏高低差。有待确认时今日仍显示投资待确认条。
- **原因**：用户觉得左右不等高，可点的入口应放状态栏更醒目。投资进 Tab；选题只是备查所以用淡链；手头的事筛选留在右栏，不进全局顶栏。
- **影响文件**：`components/app-top-nav.tsx`、`app/(app)/dashboard/page.tsx`、PRD §6.1、README、`开发进度.md`。
- **验证**：`npx tsc --noEmit`。
- **后续**：推 main 后等 Vercel。

## 2026-08-17 · 近况更新改为在现稿上增减

- **改动**：`now_md` 不再整页覆盖。提案里写了的 `##` 章节覆盖（可加长或缩短），没写到的章节保留；标题留空等于删节。创建提案和批准时都按当前 status 合并。投资页预览显示合并结果。MCP / 编辑器 / PRD 写明「不要从空白重写」。
- **原因**：线上 /now 已有完整在做/在写/在想，月度提案只交了一节「在做」，批准会把另外两节冲掉。用户要求每次更新在现稿上增加或减少。
- **影响文件**：`lib/now-revision.ts`、MCP 月度工具、持仓提案批准、投资页预览、画像编辑器提示、PRD/README、`开发进度.md`。
- **验证**：`npx tsx --test lib/now-revision.test.ts lib/status-sections.test.ts`、`npx tsc --noEmit`。
- **后续**：推 main 后等 Vercel。已有 pending 短稿不必重提，批准时会自动补上现稿里没写到的章节。确认近况后再批准。Claude 重连 MCP 才会读到新工具说明。

## 2026-08-17 · 16 仓并回 8 桶，白名单与持仓表解耦

- **改动**：一次性脚本把 16 个单只标的合并为 `CN-CPO` / `CN-MEM` / `CN-EQUIP` / `US-SEMI` / `US-MEM` / `QQQ` / `GOLD` / `CASH`，金额求和、标的写入 thesis_md，总资产仍为 788,612。`get_holding_buckets` 改为返回代码里的固定白名单，不再从活跃持仓推导。创建月度提案只写 `holding_proposals`；批准才写 holdings，且拒绝非白名单 symbol。投资页添加活跃仓也必须是这 8 个桶。顺手拒绝了用 `A-CPO` 等错误桶名的 pending #3。
- **原因**：提案 #2 实际是批准而非拒绝，拆仓快照写入了 holdings；白名单又从 holdings 派生，合并后的 `CN-CPO` 会被拒，形成死锁。
- **影响文件**：holding-buckets、holding-bucket-merge、merge 脚本、MCP、持仓 POST/PATCH、批准路径、PRD/README、`开发进度.md`。
- **验证**：`npx tsx --test lib/holding-buckets.test.ts lib/holding-bucket-merge.test.ts lib/status-sections.test.ts`、`npx tsc --noEmit`；已对生产库跑 `npm run merge:holdings:2026-08`，写回后 8 桶合计 ¥788,612。
- **后续**：推 main。Claude 重连 MCP 后应看到 8 个桶，即可提交 8 月正式提案。不要用任务清单里的 `A-CPO`，用 `CN-CPO`。微信推送仍未做。

## 2026-08-17 · 持仓桶白名单、审批直达链接、待审提示

- **改动**：作品墙种子改为 `onConflictDoNothing`，避免 Vercel 构建撞 `projects_slug_unique`。月度持仓必须用当前活跃大类桶 symbol，非法 symbol 拒绝并回传合法列表；新增 `get_holding_buckets`。MCP 提案返回带审批 URL。顶栏画像角标保留，有投资待审时显示投资角标；今日页待确认拆成画像/投资两条。同月新提案自动拒绝旧 pending。pending 超过 7 天标过期且不可批准。审计仍不写入 status（近况写者是 now_md 或手动 status 提案）。未做微信推送。
- **原因**：8 月审计按个股拆仓造成假清仓；提案没有入口提示；Vercel 种子重复插入失败。
- **影响文件**：seed-projects、holding-buckets、MCP、持仓/画像提案批准、导航、今日、投资页、README、`开发进度.md`。
- **验证**：`npx tsx --test lib/holding-buckets.test.ts lib/status-sections.test.ts`、`npx tsc --noEmit` 均通过。
- **后续**：推 main 后等 Vercel。Claude 重连 MCP。微信推送仍未做。同月重提会把旧 pending 标为 rejected（不是独立 superseded 状态）。过期按创建时间计算，库里仍是 pending。

## 2026-08-17 · 近况只留一份，月度审计同时提交 now_md

- **改动**：status 不再分内部/公开两节作为产品要求；今日与 /now 读 `statusContentForNow`（旧稿仍优先公开节，避免把内部稿推到网站）。`propose_monthly_investment_update` 增加必填 `now_md`：近况来自本轮对话策展（先读 get_profile / get_projects），不是从持仓或台账自动拼。投资页批准后同一节点写持仓、审计和 status，并同步 /now。不再生成 audit-sync 投资纪律联动提案。MCP/编辑器写明公开写作约束（可写技术栈与阶段，不写同事真名、具体业务、金额）。
- **原因**：两份近况维护成本高；隐私应靠写作标准，而不是再做一层文档。月度审计是自然的近况刷新节点。
- **影响文件**：MCP、持仓提案批准、status-sections、投资提案 UI、画像编辑器、PRD/README、`开发进度.md`。
- **验证**：`npx tsx --test lib/status-sections.test.ts`、`npx tsc --noEmit`。Claude 需重连 MCP 才能看到 `now_md`。
- **后续**：推 main 后等 Vercel。下一次月度批准会用 `now_md` 覆盖整层 status；旧「内部状态」里还想留的内容应先迁到 core / investing / relationship。

## 2026-08-17 · 今日近期状态与网站 /now 同源

- **改动**：今日「近期状态」改为渲染 status「公开状态」节（去掉 season/headline frontmatter，headline 作一行引语），与 koocuu.com `/now` 同源；旁链到网站。提取公开节时不再被节内的 `## 在做` 截断（原先同步可能只写出 frontmatter）。内部状态仍只在画像/MCP。月度审计仍只提案改内部「投资」段，不自动改 /now。
- **原因**：两处看起来像同一张近况，实际读了不同节；嵌套标题还会把公开节截断。
- **影响文件**：`lib/status-sections.ts`、`lib/status-sections.test.ts`、`app/(app)/dashboard/page.tsx`、PRD §6.1、README、`开发进度.md`。
- **验证**：`npx tsx --test lib/status-sections.test.ts`、`npx tsc --noEmit`。部署后今日应看到与 /now 相同的在做/在写/在想，而不是内部主线/投资/基调。
- **后续**：推 main 后等 Vercel。若库里公开节曾被截断过，需在画像里把完整公开近况保存一次才会重新同步网站。

## 2026-08-17 · 今日页去掉作品摘要

- **改动**：首页左侧不再列出作品短名单；`ProjectStrip` 删除。作品只在顶栏「项目」页。
- **原因**：已有项目 Tab，今日再放一份是重复。
- **影响文件**：`app/(app)/dashboard/page.tsx`、删除 `components/dashboard/project-strip.tsx`、PRD §6.1、README、`开发进度.md`。
- **验证**：`npx tsc --noEmit`。部署后今日页左侧应是状态 → 待确认 → 仓位，无作品列表。
- **后续**：推 main 后等 Vercel。

## 2026-08-16 · 公开状态支持可选 headline（给网站首页此刻一行）

- **改动**：status 编辑器提示补一句：公开状态 frontmatter 可加可选 `headline`，同步后显示在 koocuu.com 首页此刻一行。`website-sync.ts` 注释与 `seed-demo.ts` 示例同步。同步逻辑本身仍是整节 PUT，不改字段映射。
- **原因**：cu-site 主页改版需要一句 Now 引用；`current.md` 会被本仓库覆盖，必须在源头写这个字段，否则下次同步会丢掉。
- **影响文件**：`components/profile/profile-editor.tsx`、`lib/website-sync.ts`、`scripts/seed-demo.ts`、`开发进度.md`。
- **验证**：文案/注释改动，未跑 tsc。网站侧解析已在 cu-site 落地（缺字段回退 i18n）。
- **后续**：生产画像「公开状态」节补上 `headline: 让个人系统稳定产出，而不是继续加零件`（或更新后的一句话），再保存一次才会进线上 `/now`。

## 2026-08-14 · 作品页去掉锚定和重复海报墙

- **改动**：点目录只切换当前详情，不再 `scrollIntoView`。删掉目录下方那排重复小海报，页面只保留「目录 + 当前一件」。
- **原因**：想连看几件时，锚定会把人拽离目录；小海报和目录是同一份名单，没有新信息。
- **影响文件**：`components/projects/project-wall.tsx`、PRD §6.4、README、`开发进度.md`。
- **验证**：`npx tsc --noEmit`。部署后在 `/projects` 连点几个名字，页面应原地换详情、不滚动。
- **后续**：推 main 后等 Vercel。

## 2026-08-13 · 作品墙去掉在做/上线/暂停，改成目录+海报

- **改动**：作品墙不再按在做/上线/暂停分栏或筛选。页面改成大字目录定位 + 焦点海报 + 其余小海报。编辑器只保留领域轻标签。MCP `get_projects` 改为无过滤的扁平列表。DB `status` 列保留兼容，界面与 API 不再读写。
- **原因**：流程状态把作品墙做成看板，和「给人看也给 AI 读」的对象墙错位。
- **影响文件**：`components/projects/project-wall.tsx`、`lib/project-meta.ts`、`lib/queries/projects.ts`、`lib/projects-display.ts`、`app/api/projects/*`、MCP、search、seed、PRD v2.2、README。
- **验证**：`npx tsc --noEmit`。未在浏览器点选目录做视觉验收（部署后看 `/projects`）。
- **后续**：P0 仍是画像提案按节确认（MCP 已按节 patch，UI 仍整层 textarea）。Skill 合集仍等状态策展之后。推 main 后等 Vercel。

## 2026-08-13 · Dashboard 信息架构改革（今日 / 项目 / 画像）

- **改动**：顶栏改为「今日 / 项目 / 画像」；选题退出导航与首页，仅 `/topics` 备查。新增 `projects` 表与作品墙（在做/已上线/暂停），人和 MCP `get_projects` 读同一份。今日去掉选题卡片与 AI 动态墙，状态区不再套卡片；忙时台账主位、闲时提示可以不管。热更新用首屏 revision 对照，变更时短暂提示「内容已更新」。
- **原因**：产品主场是画像确认 + 作品，不是选题工厂；台账随忙闲起伏；项目要给 AI 读但不能写进画像层。
- **影响文件**：PRD v2.1、README、schema/`drizzle/0011_projects.sql`、MCP、search/export/live-revision、导航、今日页、`/projects`、seed:projects、热更新、`package.json` 的 `vercel-build`（部署时自动 migrate / trgm / seed:projects）。
- **验证**：`npx tsc --noEmit` 通过。生产迁移/索引/作品种子改由 Vercel `vercel-build` 在每次部署时自动执行（幂等：已有 slug 不覆盖）。
- **后续**：推送到 main 后等 Vercel；Claude 重连 MCP 以看到 `get_projects`。P0 提案 hunk 编辑仍待做。Skill 合集等项目墙和状态策展之后。

## 2026-08-13 · 忙闲台账与 Skill 合集口径（未改代码）

- **改动**：采纳用户补充：工作台账忙时很想用、闲时没用，主要是负荷季节不是功能失败；系统改革 → 项目墙 → 更新状态 → 再沉淀个人 skill 合集。诊断 canvas 增加「忙闲与 Skill」页，顺序表加 P4。
- **原因**：闲时空台账若仍占主位，整站会像荒掉；skill 若从画像/选题直接生成，会变成第二份会漂的事实，和「有数据就做 UI」同一陷阱。
- **产品口径**：
  - 今日 = 季节性捕捉面（忙时浮起，闲时退到状态和项目墙后）；空态不暗示失败。
  - 画像 = 你是谁/现在怎样（事实唯一来源，MCP 读取）。
  - 项目 = 作品对象；skill 挂在重复流程上，不挂在每条待办上。
  - Skill 住 Cursor 个人目录；Console 最多做索引。skill 写「先 get_profile 再按流程做」，不复制画像正文。
  - 状态仍提案策展，不由项目墙自动生成。
- **影响范围**：后续 PRD 信息架构与 P1 空态、P3 项目墙、P4 skill。未改业务代码。
- **验证**：只读讨论，无运行时验证。
- **后续**：用户若点头开改，仍先 P0（按节确认 + 热更新可感知）。

## 2026-08-13 · 工作台手感诊断（未改代码）

- **结论**：用不顺手不是心理作用。产品被建成「人生控制台」，日用闭环其实是「给 AI 用的病历本」。选题是供稿作业，和第一人称写欲错位；画像 MCP 能局部 patch，UI 只能改整层；热更新 4s 轮询仍像坏的，因为首次探测故意不 refresh，且刷新无提示。
- **原因**：对照代码、使用习惯、系统内容和成熟产品后形成。详情见对话旁 canvas「Console 用不顺手：诊断」。
- **关键证据**：
  - `LiveRefresh` 首次 poll 只记 revision、不 `router.refresh()`；提案编辑是整层 textarea；工作页 redirect 回首页；顶栏只有 CONSOLE / 选题 / 画像。
  - 公众号近稿（AI 病历本、删提问记录、陶哲轩问笨问题、旅行/面试/健身）全是亲身经历，不是雷达打分题。
  - 捋捋、决策助手、选本共创已上线，Console 没有作品对象，和公司任务混在工作台账。
  - koocuu.com `/now` 仍是「正在更新」，公开状态并未成为日用面子。
- **影响范围**：产品方向决策。未改业务代码与 PRD；若采纳则需改 PRD 信息架构（今日 / 项目 / 画像，选题降级）。
- **建议顺序（待确认）**：P0 提案按节确认 + 热更新可感知；P1 导航重组并用项目替换选题一等位置；P2 首页减法；P3 项目墙可视化。不要在现架构上直接加第四个 Tab。
- **验证**：只读排查，无运行时验证。
- **后续**：等用户拍板后再动 P0；未拍板前不改 PRD 范围。

## 2026-08-03 · 画像写入工具增加「主动提议」指令

- **改动**：仅修改 `propose_profile_patch` / `propose_profile_update` 的 description，追加主动识别提示：对话中出现值得长期记住的稳定事实、原则性判断、明确偏好/状态变化时，应主动调用工具提议，不必等用户说「更新画像」；仍需 dashboard 批准才生效。不新增工具、无外部依赖。
- **原因**：任何接 MCP 的 AI 在普通对话里就能主动记画像，而不是被动等指令。
- **影响文件**：`app/api/mcp/route.ts`、PRD/README 相关说明、`开发进度.md`
- **验证**：改后本地核对 description 文本；部署后需客户端重连 MCP 以刷新 tools/list。验收：含「换了新想法/立场/偏好」的对话中，AI 应主动 `propose_profile_patch`。

## 2026-07-24 · 加固 Dashboard 热更新（轮询/缓存）

- **现象**：Claude 经 MCP 提交提案后，开着的 Dashboard 看不到角标/待确认条自动更新。
- **排查**：`getLiveRevision` 在提案内容变化时指纹会变（本地对生产库验证 `changed true`）；更可能是客户端 12s 间隔偏慢 + layout 未 `force-dynamic` 导致 `router.refresh()` 吃到偏旧 RSC。
- **改动**：轮询 12s→4s；layout 加 `dynamic = "force-dynamic"`；fetch 加时间戳与 no-cache；窗口 focus 时立即探测；API 响应头加强 no-store。
- **影响文件**：`components/live-refresh.tsx`、`app/(app)/layout.tsx`、`app/api/live-revision/route.ts`
- **验证**：`getLiveRevision` 指纹变更已测；部署后请硬刷新 Dashboard 一次加载新客户端，再让 Claude 提交提案，约 4s 内应自动刷。
- **交接**：若仍无刷新，打开 DevTools Network 看 `/api/live-revision` 是否每 4s 200 且 revision 字符串在提交后变化。

## 2026-07-23 · 修复 Claude connecting error（SSE 分帧被正则吃掉，真正根因）★

- **时间线定位**：加 compat 包装**之前**连接器能连上（仅"no tools"），**之后**才变成"连接报错" → 锁定 compat 层引入回归。
- **根因**：`sanitizeSseBody` 用跨行正则 `/^data:\s*(\{.*\})\s*$/gm`，其中 `\s*$` 会**贪婪吃掉 SSE 事件终止符 `\n\n`**（本地验证：60 字符两事件→57 字符，两事件被并成畸形块，尾部终止符也丢）。`res.text()`/undici 不在乎分帧，但 Claude 的严格 SSE 解析器等不到完整事件 → "returned an error when connecting"。
- **改动**：`sanitizeSseBody` 改为逐行处理，只重写 `data:` 行的 JSON，其余行（尤其空行终止符）原样保留。
- **影响文件**：`lib/mcp-claude-compat.ts`、`scripts/test-mcp-claude-compat.ts`（新增终止符/多事件分帧断言）。
- **验证**：`npx tsx scripts/test-mcp-claude-compat.ts`、`npx tsc --noEmit`。

## 2026-07-23 · 剔除重发响应的逐跳头（chunked 等）

- **审计**：直接在库里造 OAuth access token，端到端复现 Claude 握手（HEAD→initialize→notifications/initialized→tools/list）全部 200/202、tools/list 返 7 个工具，证明服务端主逻辑无误。
- **改动**：`cleanHeaders` 在重发响应时剔除 `transfer-encoding`/`content-length`/`content-encoding`/`connection`/`keep-alive`。注：Vercel 对无 content-length 的 body 仍会重新加 `transfer-encoding: chunked`（正确分帧），故此项主要是卫生性防御，非 connecting error 根因。
- **影响文件**：`lib/mcp-claude-compat.ts`、`scripts/test-mcp-claude-compat.ts`

- **审计**：直接在库里造 OAuth access token，端到端复现 Claude 握手（HEAD→initialize→notifications/initialized→tools/list）。**全部 200/202 正常，tools/list 返回 7 个工具**，证明服务端逻辑无误。
- **根因**：`withClaudeMcpCompat` 把上游 SSE 流 `res.text()` buffer 成定长字符串后重发，却保留了上游的 `transfer-encoding: chunked` 头。node/undici 宽容能读，但 Claude 的严格 HTTP 客户端按 chunk 分帧解析定长 body → 失败，报 “returned an error when connecting”（ofid_*）。
- **改动**：重发响应时统一剔除逐跳/平台重算头：`transfer-encoding`、`content-length`、`content-encoding`、`connection`、`keep-alive`（`cleanHeaders`）。
- **影响文件**：`lib/mcp-claude-compat.ts`、`scripts/test-mcp-claude-compat.ts`
- **验证**：`npx tsx scripts/test-mcp-claude-compat.ts`（新增断言：重发不保留 chunked 头）、`npx tsc --noEmit`。

## 2026-07-23 · 修复 Claude OAuth 后 HEAD /api/mcp 挂死

- **现象**：授权成功后报 “Authorization with the MCP server failed”（ofid_*）；Vercel 日志在 token 后有 `HEAD /api/mcp`。
- **根因**：`mcp-handler` 只处理 GET/POST/DELETE，**HEAD 不写响应**；Next 把 HEAD 交给同一 handler 后连接挂起，Claude 判定授权失败。
- **改动**：显式导出快速 `HEAD`（有 token → 200，无 → 401 + WWW-Authenticate）；compat 层对 HEAD 兜底 405。
- **影响文件**：`app/api/mcp/route.ts`、`lib/mcp-claude-compat.ts`
- **验证**：本地脚本对 HEAD 8s 超时曾 Abort；部署后 HEAD 应秒回 200/401。

## 2026-07-23 · 修复 Claude 授权后 connecting error（202 body 被消费）

- **现象**：OAuth 成功，但报 “authorized, but dashboard returned an error when connecting”（ofid_*），工具仍为空。
- **根因**：`withClaudeMcpCompat` 对 `notifications/initialized` 的 **202 空 body** 调用了 `res.text()` 后又把已消费的 Response 原样返回，Claude 读 body 失败。
- **改动**：202/204/空 body 一律重建 `new Response(null, …)`；其它响应也重建 body，不再回传已读流。
- **影响文件**：`lib/mcp-claude-compat.ts`、`scripts/test-mcp-claude-compat.ts`
- **验证**：`npx tsx scripts/test-mcp-claude-compat.ts`；部署后 Claude Disconnect/重连。

## 2026-07-23 · Dashboard 自动热更新（revision 轮询）

- **改动**：新增 `GET /api/live-revision` 聚合提案/画像/持仓/工作/选题版本号；`(app)` 布局挂载 `LiveRefresh`，可见标签页每 12s 轮询，版本变化才 `router.refresh()`；切回前台立即探测。
- **原因**：Claude 经 MCP 提交提案后，开着的 Dashboard 需手动刷新才能看到待确认条与角标。
- **影响文件**：`lib/live-revision.ts`、`app/api/live-revision/route.ts`、`components/live-refresh.tsx`、`app/(app)/layout.tsx`、`PRD-个人知识库系统.md`
- **验证**：`npx tsc --noEmit`；本地可打开 Dashboard 后 MCP 提交提案，约 12s 内角标/待确认出现。部署后同验。

## 2026-07-23 · Claude 连接器显示「no tools available」

- **现象**：Claude Connectors 显示已连接（可 Disconnect），但文案为 “This connector has no tools available.”
- **排查**：OAuth 正常（#12 lastUsed 与截图时间吻合）；用 API token 调 `tools/list` 返回 7 个工具。根因是 `@modelcontextprotocol/sdk@1.26` 在 initialize/tools/list 里带了 `listChanged` / `title` / `execution` / `$schema`，Claude 客户端会**静默丢弃全部工具**（anthropics/claude-code#25081）。
- **改动**：`withClaudeMcpCompat` 清洗响应；并保留此前 path-based OAuth PRM 补全。
- **影响文件**：`lib/mcp-claude-compat.ts`、`app/api/mcp/route.ts`、`app/.well-known/oauth-*/[...path]/route.ts`、`开发进度.md`
- **验证**：`npx tsx scripts/test-mcp-claude-compat.ts`、`npx tsc --noEmit`；待部署后 Disconnect → 重新连接，应看到 7 个工具。
- **清理**：诊断用 API token `diag-mcp-tools-list`（id=9）可在 Tokens 页撤销。

## 2026-07-23 · 修复 MCP OAuth path-based 元数据 404

- **问题**：线上 `/.well-known/oauth-protected-resource` 与 AS 根路径 200，但 RFC 9728 path-based `…/api/mcp` 后缀返回 404；新版 Claude/Cursor 等客户端优先探测该路径，导致「连不上 MCP」。
- **改动**：补 catch-all 路由；`withMcpAuth` 的 `resourceMetadataPath` 指向 `/api/mcp` 后缀 PRM。
- **影响文件**：`app/.well-known/oauth-protected-resource/[...path]/route.ts`、`app/.well-known/oauth-authorization-server/[...path]/route.ts`、`app/api/mcp/route.ts`
- **验证**：本地路由已加；待部署后 curl 确认 PRM_path / AS_path 均为 200，再在客户端重新授权/重连。
- **交接**：若仍失败，先看 Console「授权」页 refresh 是否已过期（30 天），过期需重新 OAuth。

## 2026-07-23 · propose_profile_patch 支持 replace_section

- **改动**：`propose_profile_patch` 新增 `replace_section`：用完整 section Markdown（含目标 `##` 标题）整节替换；section 不存在时追加到层末。原有 add/update/delete 不变。
- **原因**：风格库等场景需要换整节，不必 `propose_profile_update` 重发整层。
- **影响文件**：`lib/profile-patch.ts`、`lib/profile-patch.test.ts`、`app/api/mcp/route.ts`、README
- **验证**：`npm run test:profile-patch`、`npx tsc --noEmit`

## 2026-07-20 · 画像层结构重组（四层定稿）

- **改动**：`creative`/`milestones`/`public`/`private` 合并迁移为 `core` / `status` / `investing` / `relationship`；status 固定「内部状态」「公开状态」两节；`/now` 改为只同步公开状态节；MCP `get_profile` 默认返回全部四层，新增 `list_profile_layers`。
- **原因**：层边界与分发默认范围最终定稿；公开近况不再独立成层，关系内容统一进 relationship。
- **影响文件**：`lib/db/schema.ts`、`lib/profile-meta.ts`、`lib/status-sections.ts`、`lib/website-sync.ts`、`app/api/mcp/route.ts`、提案/层 API、画像 UI、seed、README、PRD；迁移脚本 `scripts/migrate-profile-layers-v3.ts`（已在生产库执行）。
- **验证**：迁移后库内仅四层且字数完整；`npx tsc --noEmit`；待部署后验收 MCP 与 /now。
- **交接**：若仍有 `layer=public|private|creative` 的 pending 提案，需手动拒绝后用新层重提。

## 2026-07-23 · 选题展示不可逆性（配合雷达 v2.3）

- **改动**：`TopicCandidate` 新增 `storyline` / `irreversible`；`/topics` 详情展示「线索A/B · 不可逆/稳定循环」；稳定循环（B+false）显示琥珀色警示；MCP/导出 Markdown 同步输出线索行。
- **原因**：topic-radar v2.3 对碳基线索 B 的稳定循环素材强制降权，Console 需让人一眼看出「写得出还不错、难写出震撼」。
- **影响文件**：`lib/queries/topics.ts`、`lib/topics-display.ts`、`app/(app)/topics/page.tsx`
- **验证**：`npx tsc --noEmit` 通过；老批次无字段时不渲染，向后兼容。

## 2026-07-16 · 选题展示安全核实标注（配合雷达 v2.1）

- **改动**：`TopicCandidate` 新增 `caveat` 字段；`/topics` 详情页以琥珀色告警框展示、首页预览与 MCP/导出 Markdown 也输出该核实提醒（安全/漏洞类素材写作前须查证原文）。
- **原因**：topic-radar v2.1 对安全/漏洞类素材不再一票排除，改为附带 `⚠` 核实标注，Console 需醒目呈现，避免二次转述失真。
- **影响文件**：`lib/queries/topics.ts`、`lib/topics-display.ts`、`app/(app)/topics/page.tsx`、`components/dashboard/topic-radar-panel.tsx`
- **验证**：`npx tsc --noEmit` 通过；无 caveat 的候选不渲染该行，向后兼容。

## 2026-07-16 · 选题展示锚点（配合雷达 v2）

- **改动**：`TopicCandidate` 新增 `anchor`/`anchor_score`/`angle_authenticity`/`heat` 字段；`/topics` 详情页与首页「今日选题」预览、以及 MCP/导出用的 `formatTopicBatchMarkdown` 均展示「锚点 · …」行与三项分（锚点/真实/热度 各 x/10）。
- **原因**：topic-radar 评分规则升级为 v2（锚点检测一票否决 + 三项打分），生产库 batch #6 起候选已带 anchor 字段，Console 需同步呈现，帮助人工判断该选题有没有可落地的具体锚点。
- **影响文件**：`lib/queries/topics.ts`、`lib/topics-display.ts`、`app/(app)/topics/page.tsx`、`components/dashboard/topic-radar-panel.tsx`
- **验证**：`npx tsc --noEmit` 通过；字段向后兼容（老批次无 anchor 时不渲染该行）。部署后 `/topics` 才会显示锚点行。

## 2026-07-17 · Patch 兼容内联粗体条目

- **问题**：investing 层真实条目使用 `**标题**: 正文` 同行格式，原解析器只接受独立 `**标题**` 或 `### 标题`，导致合法内容连续报格式错误。
- **修复**：局部画像 patch 同时识别半角/全角冒号的内联粗体条目；标题仍为完整文本精确匹配，重复标题仍拒绝猜测。
- **验证**：以 investing「已知行为弱点(重要)」真实结构复现并覆盖新增/修改；`npm run test:profile-patch`、`npx tsc --noEmit`、`npm run build`。

## 2026-07-15 · MCP 画像局部提案与单一草稿

- **改动**：新增 `propose_profile_patch`，支持在指定 `##` 分区内按 `### 标题` 或独立 `**标题**` 精确增删改单条内容。
- **连续修改**：同一 MCP 调用方对同一层的后续 patch 基于现有 pending 候选稿继续修改，并更新原 proposal；其他来源已有 pending 时拒绝自动合并，避免多份候选互相覆盖。
- **安全边界**：只生成待确认提案，不直接写画像；缺失、重复 section/anchor 均明确报错；代码块中的伪标题不参与定位。
- **影响文件**：`lib/profile-patch.ts`、`lib/profile-patch-proposals.ts`、`app/api/mcp/route.ts`、README、PRD
- **验证**：`npm run test:profile-patch`、`npx tsc --noEmit`、`npm run build`

## 2026-07-14 · MCP 可读选题库

- **改动**：新增 MCP 工具 `get_topic_batch`（可选 `account` 过滤）；格式化输出与 `/topics` 同源的最新 `topic_batches`。
- **原因**：选题不在画像层，原 MCP 读不到；写稿 AI 需要直接拉候选。
- **影响文件**：`app/api/mcp/route.ts`、`lib/topics-display.ts`、`README.md`
- **验证**：`npx tsc --noEmit`；部署后刷新 Claude/Cursor 连接器工具列表

## 2026-07-13 · 选题页与中文标题

- **改动**：首页「今日选题」改为精简预览（每账号最多 2 条）+ 链到 `/topics` 全文页；顶栏增加「选题」；topic-radar 打分新增 `title_zh`。
- **原因**：首页小框塞全文难读；源标题多为英文，需要中文工作标题。
- **验证**：`npx tsc --noEmit`；topic-radar Actions `29223454508` ✅ → batch #2；`GET /api/topic-batches` 10/10 候选均有中文 `title_zh`。刷新首页与 `/topics` 即可。

## 2026-07-13 · 选题改首页区块（非画像提案）

- **改动**：新增 `topic_batches` 表与 `POST/GET /api/topic-batches`；首页「今日选题」展示最新批次；撤回误加的画像层 `topics`。
- **原因**：画像提案只应用于画像更新；选题雷达应是首页可浏览的候选列表。
- **影响文件**：`lib/db/schema.ts`、`drizzle/0010_topic_batches.sql`、`lib/queries/topics.ts`、`app/api/topic-batches/route.ts`、`middleware.ts`、`components/dashboard/topic-radar-panel.tsx`、`app/(app)/dashboard/page.tsx`、画像层相关回退
- **验证**：`npx tsc --noEmit`；需执行 `npm run db:migrate` 后部署
- **你后续**：部署 + 迁移；拒绝旧的 topics 画像提案；再跑一次 topic-radar Actions

## 2026-07-13 · topic-radar 对接

- **改动**：新增画像层 `topics`（内容选题候选）；MCP / 粘贴协议 / propose API 校验文案同步支持；完整版分发默认包含 `topics`，通用版不含。
- **原因**：独立服务 topic-radar 每日通过已有 `POST /api/profile/proposals` 写入 pending proposal，不新建并行写入通道。
- **影响文件**：`lib/db/schema.ts`、`lib/profile-meta.ts`、`app/api/mcp/route.ts`、`app/api/profile/proposals/route.ts`、`lib/profile-update-parser.ts`、`lib/profile-update-protocol.ts`
- **验证**：`npx tsc --noEmit` 通过（无 DB migration：layer 为 text）
- **你后续**：部署本改动；在设置页创建 write token（名如 `topic-radar`）交给 topic-radar 的 `CONSOLE_API_TOKEN`；每天在「画像提案」批准 `topics` 即可

---

# 开发进度 · Console v2

> 当前实施口径依据 `PRD-个人知识库系统.md` v2.0,最后更新:2026-07-11。

## 总览

| Phase | 内容 | 状态 |
|---|---|---|
| Phase 1 | 登录、首页、工作台账、PWA | ✅ 完成 |
| Phase 2 | 五层画像、分享页、提案、持仓 | ✅ 完成 |
| Phase 3 | 远程 MCP、OAuth、月度投资审计 | ✅ 完成 |

## 当前产品口径

- 首页桌面双栏、移动单列;底部 Tab 和独立设置页已删除。
- 工作事项只有 `someday / scheduled / in_progress / done` 四态;点击圆点前进,支持跨栏拖拽、置顶和轻分类。
- 快速录入默认进入“想做未做”;置顶和状态是独立字段。
- 画像分发以 `/c/<slug>` 分享页和复制文本为主;MCP `get_profile` 返回无包装纯净内容。
- 画像写入统一走 proposal → diff → 批准;粘贴协议保持严格解析。
- 持仓以 `amount_cny` 为主数据,名称和金额可编辑;比例由全部资产金额自动计算。
- 现金以 `CASH` 显式计入总资产;环图切片合计 100%,中心显示非现金已投资比例。
- 2026-07 当前快照:总资产 `¥904,646`,现金 `5.53%`,已投 `94.47%`。
- 月度投资更新只通过 MCP `propose_monthly_investment_update` 提交;固定四段审计与完整金额持仓一次批准、同节点落库。
- MCP 另外提供 `get_profile`、`search_entries`、`propose_profile_patch`、`propose_profile_update`;支持 OAuth 和静态 Bearer token。
- Neon 迁移已到 `0009_holding_amount_monthly_review`;同步脚本会更新 7 月金额并刷新当月快照。

## 验证状态

- ✅ `npx tsc --noEmit`
- ✅ `npm run build`
- ✅ 金额样例计算:九项 `¥904,646`,切片合计 `100.00%`,现金 `5.53%`,已投 `94.47%`
- ✅ `git pull --rebase origin main` 已确认远端最新

## 线上硬验收

- [x] Neon 已执行金额/月度提案迁移并同步 2026-07 数据
- [x] 7 月月度审计保存金额快照
- [x] Claude.ai OAuth 可连接 `/api/mcp`
- [ ] 部署本次“已投比例”修正后,首页与投资页中心显示 `94.47%`
- [ ] Claude.ai 刷新连接器后能看到并调用 `propose_monthly_investment_update`
- [ ] 用一条测试月度提案走完:提交 → 投资页 diff → 批准 → 持仓与复盘同时更新
- [x] 私有仓库 `koocuu/CuDashboard-backup` 已创建,2026-07-11 手动初始备份成功(41 个 Markdown)
- [ ] 在 Vercel Production 配置仅限该仓库的 fine-grained `GITHUB_BACKUP_TOKEN`,再验证每日 Cron

## 月更操作

1. 把当月真实金额和审计材料交给支持 MCP 的 AI。
2. AI 调用 `propose_monthly_investment_update`,必须包含全部资产及 `CASH`。
3. 在投资页核对总资产、每项金额、比例变化和四段总结。
4. 批准后系统同步持仓并固化当月快照;不再手工粘贴复盘。
